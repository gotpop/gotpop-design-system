name: Deploy to Netlify

on:
  push:
    branches: [master]

jobs:
  deploy:
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '22'
          cache: 'yarn'

      - name: Install dependencies
        run: yarn install --frozen-lockfile

      - name: Build system package
        run: yarn workspace @gotpop/system build
        env:
          STORYBLOK_ACCESS_TOKEN: ${{ secrets.STORYBLOK_ACCESS_TOKEN }}

      - name: Build Storybook
        run: yarn workspace storybook build
        env:
          STORYBLOK_ACCESS_TOKEN: ${{ secrets.STORYBLOK_ACCESS_TOKEN }}

      - name: Deploy to Netlify
        run: |
          npm install -g netlify-cli
          # Temporarily rename netlify.toml to avoid monorepo config conflicts
          mv netlify.toml netlify.toml.bak
          netlify deploy \
            --dir=apps/storybook/storybook-static \
            --site=${{ secrets.NETLIFY_SITE_ID }} \
            --auth=${{ secrets.NETLIFY_AUTH_TOKEN }} \
            --prod
          # Restore netlify.toml
          mv netlify.toml.bak netlify.toml